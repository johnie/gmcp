# Google OAuth2 credentials JSON file from Google Cloud Console
GOOGLE_CREDENTIALS_PATH=/path/to/credentials.json

# Path where OAuth2 tokens will be stored after authentication
GOOGLE_TOKEN_PATH=/path/to/token.json

# Comma-separated API scopes (short names or full URLs)
# Examples:
#   gmail.readonly                    - Read-only Gmail access
#   gmail.readonly,calendar.readonly  - Read-only Gmail + Calendar
#   gmail.readonly,gmail.send,calendar.events - Gmail read/send + Calendar events
#   gmail.readonly,gmail.modify,gmail.send,gmail.labels,calendar - Full access
GOOGLE_SCOPES=gmail.readonly,calendar.readonly

# Optional: Logging configuration
# LOG_LEVEL=info
# LOG_PRETTY=false
